import React from "react";

import styles from "./RegionsButton.module.css";

const RegionsButton = () => {
  return (
    <button className={styles.region} type="button">
      Region
    </button>
  );
};

export default RegionsButton;
